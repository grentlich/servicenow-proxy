{
  "name": "servicenow-proxy",
  "version": "1.0.0",
  "description": "Proxy for ServiceNow API requests",
  "main": "index.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "axios": "^1.9.0"
  }
}
